# 安全说明

[English](SECURITY.md) · 简体中文 · [返回首页](README.zh.md)

## 报告安全问题

请通过仓库的 **Security → Report a vulnerability** 私下报告安全问题，不要直接发布公开 Issue。我们会尽量在几天内回复。

## 安全边界与设计

自定义 UI 使用非持久 WKWebView，仅提供包内资源，CSP 禁止联网、远程脚本和 iframe。Bridge 只接受当前主页面的有界请求，身份由宿主绑定；剪贴板和任务执行需要清单声明能力。密码不返回网页。插件存储按来源隔离，每包 5 MiB；任务请求使用私有 JSON 文件，输出限制 4 MiB，并遮蔽跨分块密码。详细契约见[插件 UI 文档](docs/plugin-ui.zh.md)。这些限制不构成脚本或二进制的系统沙盒。

AnyWhere 由非沙盒的 Developer ID 主应用和沙盒内的 Finder Sync 扩展组成。

- **脚本以当前用户权限执行。** 预设、自建动作和扩展包脚本与终端中执行的程序具有相同信任等级。启用前应审阅脚本。应用通过 `/bin/zsh "$script" "$@"` 执行，路径作为参数及 `ANYWHERE_PATHS` 传递，不将选中路径拼成命令或交给 `eval`。
- **扩展包导入后只读且默认禁用。** Git 来源执行浅克隆，本地来源复制文件夹；导入过程不执行脚本。审阅界面展示声明脚本和其他非元数据文件，并标记隐藏脚本、可执行文件和二进制，因为动作脚本可以引用包内其他文件。用户审阅后逐个启用动作。
- **主应用与扩展之间的快照不是权限隔离边界。** 菜单快照和点击请求通过 `DistributedNotificationCenter` 传递，同一用户下的其他进程也能访问该通信。它们本身已拥有同等的本地文件访问权限。点击在主应用中会再次依据本地配置校验：动作必须存在、已启用，且脚本路径有效，不能只靠伪造菜单快照指定任意脚本执行。
- **插件密码在本地加密。** CryptoKit 使用 AES-256-GCM 加密和认证 `~/Library/Application Support/AnyWhere/PrivateData/` 中的密码数据，随机生成 256 位密钥。目录权限为 `0700`，密钥及密文文件为 `0600`。配置操作不访问钥匙串、不要求系统密码。备份必须同时包含密钥和密文；密钥缺失、不匹配或密文损坏时会报错，不重置原数据。以当前用户身份取得两个文件的进程仍能解密，因此这不隔离同一用户下的其他进程。脚本只接收当前动作声明的配置，执行输出中原样出现的密码会被遮蔽。
- **移除隔离属性会绕过 Gatekeeper 的相应检查。** 如果自行添加删除 `com.apple.quarantine` 的动作，只对可信文件使用；它会移除下载来源／未识别开发者检查所依赖的属性。
- **系统权限按用途申请。** 导航使用自动化权限控制 Finder 或 System Events；在非 Finder 上传对话框中发送 `⌘↑` 时可能需要辅助功能权限。应用不要求完全磁盘访问权限，也不使用 App Group 容器保存配置。
- **通过 AI 或外部编辑器创建动作会直接修改本地文件。** 修改 `config.json` 和 `Scripts/` 不经过扩展包审阅流程，适合用户自己的自动化；对这些脚本也应按手写脚本的标准审阅。编写规则见[扩展包规范](docs/pack-spec.zh.md)。
